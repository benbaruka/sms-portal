import * as Module from "../../../../../src/app/(admin)/contacts/create/page";
import { renderWithProviders } from "../../../../test-utils";

describe("app/(admin)/contacts/create/page", () => {
  it("module loads", () => {
    expect(Module).toBeTruthy();
  });
});
