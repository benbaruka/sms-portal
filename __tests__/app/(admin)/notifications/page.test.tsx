import * as Module from "../../../../src/app/(admin)/notifications/page";
import { renderWithProviders } from "../../../test-utils";

describe("app/(admin)/notifications/page", () => {
  it("module loads", () => {
    expect(Module).toBeTruthy();
  });
});
