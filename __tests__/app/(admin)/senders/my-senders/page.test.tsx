import * as Module from "../../../../../src/app/(admin)/senders/my-senders/page";
import { renderWithProviders } from "../../../../test-utils";

describe("app/(admin)/senders/my-senders/page", () => {
  it("module loads", () => {
    expect(Module).toBeTruthy();
  });
});
