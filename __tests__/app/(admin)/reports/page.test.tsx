import * as Module from "../../../../src/app/(admin)/reports/page";
import { renderWithProviders } from "../../../test-utils";

describe("app/(admin)/reports/page", () => {
  it("module loads", () => {
    expect(Module).toBeTruthy();
  });
});
